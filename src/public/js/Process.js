/**
 * Created by: spurcell
 * 1/10/15
 */

"use strict";


window.Process = function (env, service) {

    var dom = $('#env-' + envId).find('td.service-' + serviceId + ' errorFlag').text(message);
};

Process.prototype.logError = function (error) {


};